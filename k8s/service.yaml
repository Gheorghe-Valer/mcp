apiVersion: v1
kind: Service
metadata:
  name: mcp-service
  labels:
    app: mcp
    app.kubernetes.io/managed-by: Helm
  annotations:
    meta.helm.sh/release-name: mcp-nest-k8s
    meta.helm.sh/release-namespace: default
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: mcp